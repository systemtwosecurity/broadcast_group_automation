# Environment (dev, qa, or prod)
ENVIRONMENT=dev

# ================================
# Bearer Tokens
# ================================
# Get your token from Chrome DevTools:
# 1. Login to https://detections.dev.s2s.ai (or qa/prod)
# 2. Open DevTools → Network tab
# 3. Make any API call
# 4. Copy the "Authorization: Bearer ..." header value (without "Bearer ")
# 
# Tokens typically expire in 1 hour. Update them as needed.

# Admin token (for sending invitations)
ADMIN_TOKEN=your-admin-bearer-token-here

# User tokens (for creating groups and sources)
# Set to "SKIP" for users you don't want to process
USER_TOKEN_SIGMAHQ=SKIP
USER_TOKEN_YARA_100DAYS=SKIP
USER_TOKEN_GOOGLESECOPS=SKIP
USER_TOKEN_AZURE_SENTINEL=SKIP
USER_TOKEN_REVERSINGLABS_YARA=SKIP
USER_TOKEN_SPLUNKSECURITY=SKIP

